# Dockerfile в директории /root/install-wizard/backend/

# Базовый образ
FROM node:18-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /usr/src/app

# Копируем package.json и package-lock.json для установки зависимостей
# Это важно, чтобы Docker мог кэшировать слои
COPY package*.json ./

# Устанавливаем зависимости
RUN npm install

# Копируем остальной исходный код приложения
# КОМАНДА НИЖЕ ОЖИДАЕТ, ЧТО ВСЕ ФАЙЛЫ В ТЕКУЩЕМ КОНТЕКСТЕ СБОРКИ (т.е. в /root/install-wizard/backend/)
# БУДУТ СКОПИРОВАНЫ В /usr/src/app ВНУТРИ КОНТЕЙНЕРА
COPY . .

# Открываем порт, на котором слушает ваше приложение (например, 3001, если это API)
EXPOSE 3001

# Команда для запуска приложения
CMD [ "node", "src/index.js" ] # Или 'npm', 'start' если у вас есть скрипт старта
