# VSS DCI Telegram Bot

Интеграция Telegram бота для мониторинга и управления VSS DCI системой.

## Возможности

- ✅ Подключение к собственному Telegram боту
- ✅ Мониторинг статуса системы
- ✅ Получение уведомлений о событиях (ошибки, пайплайны)
- ✅ Просмотр логов через бота
- ✅ Управление пайплайнами
- ✅ Интеграция с RabbitMQ для автоматических уведомлений

## Установка

1. Установите зависимости:
```bash
cd services/dci
npm install
```

2. Получите токен бота от [@BotFather](https://t.me/BotFather):
   - Откройте Telegram и найдите @BotFather
   - Отправьте команду `/newbot`
   - Следуйте инструкциям для создания бота
   - Сохраните полученный токен

## Использование

### Подключение бота через API

```bash
# Подключение к боту
curl -X POST http://localhost:8082/api/dci/telegram/connect \
  -H "Content-Type: application/json" \
  -d '{
    "bot_token": "YOUR_BOT_TOKEN",
    "chat_id": 123456789
  }'
```

**Параметры:**
- `bot_token` (обязательный) - Токен бота от @BotFather
- `chat_id` (опциональный) - ID чата для отправки сообщений. Если не указан, будет использован ID чата, из которого отправлена команда `/start`

### Проверка статуса

```bash
# Получить статус бота
curl http://localhost:8082/api/dci/telegram/status
```

### Отправка сообщения

```bash
# Отправить сообщение через бота
curl -X POST http://localhost:8082/api/dci/telegram/send \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Тестовое сообщение",
    "chat_id": 123456789,
    "parse_mode": "Markdown"
  }'
```

### Отключение бота

```bash
# Отключить бота
curl -X POST http://localhost:8082/api/dci/telegram/disconnect
```

## Команды бота

После подключения бота, вы можете использовать следующие команды в Telegram:

- `/start` - Начать работу с ботом и сохранить ваш chat_id
- `/status` - Получить статус VSS DCI системы
- `/logs [N]` - Просмотреть последние N логов (по умолчанию 10)
- `/pipelines` - Список последних пайплайнов
- `/help` - Справка по командам

## Автоматические уведомления

Бот автоматически отправляет уведомления о следующих событиях:

1. **Критические ошибки** - Все события с уровнем `error` или `critical` из логов
2. **События пайплайнов**:
   - Запуск пайплайна (`pipeline.start`)
   - Остановка пайплайна (`pipeline.stop`)
   - Обновление статуса пайплайна (`pipeline.update`)

## Интеграция с RabbitMQ

Бот автоматически подписывается на события через RabbitMQ:

- `vss.events.pipeline.*` - События пайплайнов
- `vss.events.*.error` - Ошибки
- `vss.events.*.critical` - Критические события

## Примеры использования

### Пример 1: Подключение и отправка тестового сообщения

```javascript
// Подключение
const response = await fetch('http://localhost:8082/api/dci/telegram/connect', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    bot_token: '123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11',
    chat_id: 123456789
  })
});

// Отправка сообщения
await fetch('http://localhost:8082/api/dci/telegram/send', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    message: '🚀 VSS DCI система запущена!',
    parse_mode: 'Markdown'
  })
});
```

### Пример 2: Мониторинг через бота

1. Подключите бота через API
2. Отправьте команду `/start` в Telegram боту
3. Используйте команды для мониторинга:
   - `/status` - проверка статуса
   - `/logs 20` - последние 20 логов
   - `/pipelines` - список пайплайнов

## Хранение конфигурации

Конфигурация бота (токен и chat_id) сохраняется в базе данных PostgreSQL в таблице `telegram_bot_config`. При перезапуске сервиса бот автоматически подключается, если конфигурация сохранена.

## Безопасность

- Токен бота хранится в базе данных
- При получении статуса токен показывается частично (только последние 4 символа)
- Рекомендуется использовать переменные окружения для токена в production

## Переменные окружения

Вы можете задать токен бота через переменную окружения:

```bash
TELEGRAM_BOT_TOKEN=your_bot_token_here
```

Если токен задан в переменной окружения, бот автоматически подключится при старте сервиса.

## Устранение неполадок

### Бот не подключается

1. Проверьте правильность токена
2. Убедитесь, что бот не заблокирован
3. Проверьте логи сервиса: `docker logs vss-dci` или в консоли

### Не приходят уведомления

1. Проверьте статус бота: `GET /api/dci/telegram/status`
2. Убедитесь, что chat_id сохранен (отправьте `/start` боту)
3. Проверьте подключение к RabbitMQ

### Ошибка "Bot is not connected"

1. Подключите бота через API: `POST /api/dci/telegram/connect`
2. Проверьте статус: `GET /api/dci/telegram/status`

## API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| POST | `/api/dci/telegram/connect` | Подключение к боту |
| POST | `/api/dci/telegram/disconnect` | Отключение от бота |
| GET | `/api/dci/telegram/status` | Статус бота |
| POST | `/api/dci/telegram/send` | Отправка сообщения |

## Дополнительная информация

- [Telegram Bot API Documentation](https://core.telegram.org/bots/api)
- [node-telegram-bot-api](https://github.com/yagop/node-telegram-bot-api)

