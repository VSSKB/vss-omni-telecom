# 🚀 Быстрый запуск портала документации

## Вариант 1: Локальный запуск (для разработки)

```powershell
# Перейти в директорию портала
cd docs-portal

# Установить зависимости (только первый раз)
npm install

# Запустить портал
npm start
```

**Готово!** Откройте браузер: **http://localhost:3100**

---

## Вариант 2: Запуск через Docker Compose (рекомендуется)

```powershell
# Запустить только портал документации
docker-compose -f docker-compose.vss-demiurge-simple.yml up -d vss-docs

# Или запустить весь стек VSS (включая портал)
docker-compose -f docker-compose.vss-demiurge-simple.yml up -d
```

**Готово!** Откройте браузер: **http://localhost:3100**

---

## Проверка статуса

```powershell
# Проверить работу портала
Invoke-WebRequest -Uri "http://localhost:3100" -UseBasicParsing

# Просмотреть логи
docker logs vss-docs-portal

# Проверить статус контейнера
docker ps | Select-String "vss-docs"
```

---

## Остановка

```powershell
# Остановить локальный сервер
# Нажмите Ctrl+C в терминале где запущен сервер

# Остановить Docker контейнер
docker-compose -f docker-compose.vss-demiurge-simple.yml stop vss-docs

# Или полностью удалить
docker-compose -f docker-compose.vss-demiurge-simple.yml down vss-docs
```

---

## 🎯 Что дальше?

1. Откройте **http://localhost:3100**
2. Используйте поиск для быстрого нахождения документов
3. Просматривайте документацию по категориям
4. Наслаждайтесь удобной навигацией!

---

## ❓ Проблемы?

### Порт 3100 занят?
```powershell
# Найти процесс на порту 3100
Get-NetTCPConnection -LocalPort 3100 -ErrorAction SilentlyContinue

# Или изменить порт в docker-compose.yml
ports:
  - "3101:3100"  # Измените 3101 на нужный порт
```

### Зависимости не установлены?
```powershell
cd docs-portal
npm install
```

### Docker образ не собирается?
```powershell
# Пересобрать образ
docker-compose -f docker-compose.vss-demiurge-simple.yml build vss-docs

# Затем запустить
docker-compose -f docker-compose.vss-demiurge-simple.yml up -d vss-docs
```

---

**Готово! Наслаждайтесь порталом документации! 📚✨**

