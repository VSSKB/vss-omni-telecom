# 📞 FreeSWITCH Web Admin - Руководство пользователя

## ✅ Что установлено

### FreeSWITCH
- **Контейнер**: `vss-freeswitch`
- **Версия**: Latest (SignalWire)
- **Статус**: ✅ Запущен

### Веб-админка
- **Расположение**: `services/freeswitch-admin/`
- **Технология**: Node.js + Express + ESL (Event Socket Layer)
- **Порт**: 8088
- **Статус**: ✅ Запущена

---

## 🌐 Доступ к веб-интерфейсу

### Прямой доступ:
```
http://79.137.207.215:8088/
```

### Через Nginx:
```
http://79.137.207.215/freeswitch/
```

---

## 🎯 Возможности веб-админки

### 1. **Dashboard (Главная)**
- ✅ Статус системы в реальном времени
- ✅ Количество активных звонков
- ✅ Количество зарегистрированных пользователей
- ✅ Индикатор подключения к FreeSWITCH

### 2. **Active Calls (Активные звонки)**
- ✅ Просмотр всех активных звонков
- ✅ Информация: UUID, направление, от кого, кому
- ✅ Завершение звонка (Hangup)
- ✅ Автообновление каждые 10 секунд

### 3. **Registrations (Регистрации)**
- ✅ Список зарегистрированных SIP-пользователей
- ✅ Информация: User, Contact, Agent, Status
- ✅ Ручное обновление

### 4. **Make Call (Совершить звонок)**
- ✅ Originate call между двумя extension'ами
- ✅ Простая форма: From → To
- ✅ Автоматический переход к списку звонков после создания

### 5. **Console (Консоль)**
- ✅ Выполнение любых FreeSWITCH команд
- ✅ История команд
- ✅ Подсветка синтаксиса
- ✅ Быстрые команды в подсказке

### 6. **Sofia Status**
- ✅ Статус SIP-стека Sofia
- ✅ Информация о профилях и gateway'ях
- ✅ Обновление по требованию

---

## 📞 Порты FreeSWITCH

| Порт | Протокол | Назначение |
|------|----------|------------|
| **5060** | TCP/UDP | SIP (основной) |
| **5080** | TCP/UDP | SIP (альтернативный) |
| **8021** | TCP | ESL - Event Socket Layer (для управления) |
| **16384-16394** | UDP | RTP Media (голосовой трафик) |
| **7443** | TCP | WSS - WebSocket Secure |
| **5066** | TCP | WebRTC |

---

## 🔧 Управление FreeSWITCH

### Через веб-интерфейс:
1. Откройте http://79.137.207.215:8088/
2. Используйте вкладки для различных операций
3. Console позволяет выполнять любые команды

### Через Docker CLI:

```bash
# Подключиться к консоли FreeSWITCH
docker exec -it vss-freeswitch fs_cli

# Просмотр логов
docker logs vss-freeswitch -f

# Перезапуск
docker restart vss-freeswitch

# Остановка
docker stop vss-freeswitch

# Запуск
docker start vss-freeswitch
```

### Полезные команды в fs_cli:

```
# Показать статус системы
status

# Показать активные каналы
show channels

# Показать регистрации
show registrations

# Показать звонки
show calls

# Перезагрузить XML конфигурацию
reloadxml

# Статус Sofia-SIP
sofia status

# Завершить звонок
uuid_kill <UUID>

# Совершить звонок
originate user/1000 &bridge(user/1001)

# Показать endpoint'ы
show endpoints

# Показать кодеки
show codecs

# Выход из консоли
exit или /bye
```

---

## 🎯 Примеры использования

### 1. Проверка статуса системы
1. Откройте веб-админку
2. На главной странице увидите статус подключения
3. Зеленый индикатор = всё работает

### 2. Просмотр активных звонков
1. Перейдите на вкладку "Calls"
2. Увидите список всех активных звонков
3. Можете завершить любой звонок кнопкой "Hangup"

### 3. Совершение тестового звонка
1. Перейдите на вкладку "Make Call"
2. В поле "From" введите extension (например: 1000)
3. В поле "To" введите другой extension (например: 1001)
4. Нажмите "Make Call"
5. Автоматически перейдёте к списку звонков

### 4. Выполнение команды
1. Перейдите на вкладку "Console"
2. Введите команду (например: `status`)
3. Нажмите Enter или кнопку "Execute"
4. Результат отобразится в консоли

### 5. Проверка Sofia-SIP
1. Перейдите на вкладку "Sofia Status"
2. Нажмите "Refresh Status"
3. Увидите статус всех SIP-профилей

---

## 🔐 Безопасность

### ESL пароль:
По умолчанию FreeSWITCH использует пароль `ClueCon` для ESL.

### Изменение пароля:
1. Отредактируйте файл в контейнере:
```bash
docker exec -it vss-freeswitch vi /etc/freeswitch/autoload_configs/event_socket.conf.xml
```

2. Найдите строку:
```xml
<param name="password" value="ClueCon"/>
```

3. Измените на свой пароль

4. Обновите `.env` файл:
```
FREESWITCH_PASSWORD=your_new_password
```

5. Перезапустите FreeSWITCH:
```bash
docker restart vss-freeswitch
```

6. Перезапустите веб-админку

---

## 🎨 Интерфейс

### Цветовая схема:
- **Зелёный** 🟢 - Подключено / Успешно
- **Красный** 🔴 - Отключено / Ошибка
- **Жёлтый** 🟡 - Предупреждение
- **Синий** 🔵 - Информация

### Автообновление:
- **Статус подключения**: каждые 5 секунд
- **Активные звонки**: каждые 10 секунд
- Остальные данные обновляются вручную

---

## 📊 Интеграция с VSS

FreeSWITCH интегрирован в VSS стек и доступен через:

### Nginx Gateway:
```
http://79.137.207.215/freeswitch/
```

### Прямой доступ:
```
http://79.137.207.215:8088/
```

### ESL подключение:
```javascript
const modesl = require('modesl');
const conn = new modesl.Connection('79.137.207.215', 8021, 'ClueCon');
```

---

## 🔧 Решение проблем

### Проблема 1: Веб-админка не подключается к FreeSWITCH

**Симптомы:**
- Красный индикатор "Disconnected"
- Ошибки в консоли

**Решение:**
```bash
# 1. Проверьте, что FreeSWITCH запущен
docker ps | findstr freeswitch

# 2. Проверьте порт ESL (8021)
Test-NetConnection -ComputerName localhost -Port 8021

# 3. Проверьте логи FreeSWITCH
docker logs vss-freeswitch

# 4. Перезапустите FreeSWITCH
docker restart vss-freeswitch

# 5. Перезапустите веб-админку
cd services\freeswitch-admin
npm start
```

### Проблема 2: Звонки не проходят

**Решение:**
1. Убедитесь, что extension'ы существуют
2. Проверьте регистрации: вкладка "Registrations"
3. Проверьте Sofia: вкладка "Sofia Status"
4. Выполните в Console: `sofia status profile internal`

### Проблема 3: Порт 8088 занят

**Решение:**
```powershell
# Найти процесс
Get-NetTCPConnection -LocalPort 8088 | Select-Object OwningProcess
Get-Process -Id [PID]

# Остановить процесс
Stop-Process -Id [PID] -Force

# Или изменить порт в .env
FREESWITCH_ADMIN_PORT=8089
```

### Проблема 4: Нет звука (RTP)

**Решение:**
1. Проверьте, что порты 16384-16394 открыты
2. В консоли FreeSWITCH: `show channels`
3. Проверьте кодеки: `show codecs`

---

## 📝 API Documentation

Веб-админка предоставляет REST API:

### Endpoints:

```
GET  /api/status          - Статус системы
GET  /api/channels        - Активные каналы
GET  /api/registrations   - Регистрации
GET  /api/calls           - Активные звонки
POST /api/command         - Выполнить команду
POST /api/originate       - Создать звонок
POST /api/hangup          - Завершить звонок
POST /api/reload          - Перезагрузить конфигурацию
GET  /api/sofia/status    - Sofia-SIP статус
```

### Пример использования:

```javascript
// Получить активные звонки
fetch('http://79.137.207.215:8088/api/calls')
    .then(res => res.json())
    .then(data => console.log(data));

// Создать звонок
fetch('http://79.137.207.215:8088/api/originate', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ from: '1000', to: '1001' })
})
.then(res => res.json())
.then(data => console.log(data));

// Выполнить команду
fetch('http://79.137.207.215:8088/api/command', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ command: 'status' })
})
.then(res => res.json())
.then(data => console.log(data.result));
```

---

## 🚀 Дальнейшая настройка

### 1. Добавление SIP-пользователей
Отредактируйте конфигурацию FreeSWITCH в контейнере:
```bash
docker exec -it vss-freeswitch vi /etc/freeswitch/directory/default/*.xml
```

### 2. Настройка SIP-транков
```bash
docker exec -it vss-freeswitch vi /etc/freeswitch/sip_profiles/external/*.xml
```

### 3. Настройка диалплана
```bash
docker exec -it vss-freeswitch vi /etc/freeswitch/dialplan/*.xml
```

### 4. Добавление кодеков
```bash
docker exec -it vss-freeswitch vi /etc/freeswitch/autoload_configs/modules.conf.xml
```

---

## 📚 Полезные ссылки

- [FreeSWITCH Documentation](https://freeswitch.org/confluence/)
- [ESL Documentation](https://freeswitch.org/confluence/display/FREESWITCH/Event+Socket+Library)
- [Sofia-SIP](https://freeswitch.org/confluence/display/FREESWITCH/Sofia-SIP)
- [Dialplan](https://freeswitch.org/confluence/display/FREESWITCH/Dialplan)

---

## 🎯 Quick Start Checklist

- [x] FreeSWITCH контейнер запущен
- [x] Веб-админка запущена на порту 8088
- [x] Nginx настроен для проксирования
- [x] ESL подключение работает
- [x] Все порты открыты и доступны

**Следующие шаги:**
1. Настройте SIP-пользователей
2. Настройте диалплан
3. Подключите SIP-транки (если нужно)
4. Протестируйте звонки

---

**Версия**: 1.0  
**Дата**: 2025-12-04  
**Статус**: ✅ Полностью рабочий

